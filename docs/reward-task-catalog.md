# 獎勵與任務清冊

本文件記錄 2026-08-24 於 BS5 台灣版實機確認的任務清單，以及對應的 Maa 任務狀態。

## 介面規則

- 所有領獎入口只顯示一個 Maa 任務：`ClaimAllRewards`（領取所有獎勵）。
- 家園床與推車掛機收益已接入統一領獎流程。
- 好友、餽贈、任務、商城與精彩活動的領獎流程保留為內部 Pipeline，不再各自顯示成 Maa 任務。
- 需要玩家實際完成條件的項目才建立獨立 Maa 任務；尚未取得完整進場與停止畫面的項目維持安全骨架，不會假裝執行成功。
- 巡禮類活動固定先處理「巡禮目標」，再切回「巡禮之證」領取升級後的免費獎勵；只辨識免費欄紅色驚嘆號，排除「購買等級」與「解鎖賜福」。

## 任務頁：日常

| 遊戲條件              | Maa 任務                      |
| --------------------- | ----------------------------- |
| 今日上線登入          | 被動達成，不建立 Maa 任務     |
| 在家園商店購物 2 次   | `HomeShopPurchase`            |
| 完成日常副本 1 次     | `DailyDungeon1`               |
| 完成競技場挑戰 1 次   | `DailyArenaChallenge1`        |
| 領取聖獸試煉獎勵 1 次 | `DailyClaimBeastTrialReward1` |
| 雙影幻境擊破第 3 波   | `DailyPhantomRealmWave3`      |

## 商城：巡禮目標

### 每日目標

| 遊戲條件          | Maa 任務                                |
| ----------------- | --------------------------------------- |
| 今日上線登入      | 被動達成，不建立 Maa 任務               |
| 女神像祈願 2 次   | `PilgrimageDailyGoddessPrayer2`         |
| 完成 4 個每日任務 | `PilgrimageDailyCompleteFourDailyTasks` |
| 完成 2 個委託任務 | `PilgrimageDailyCompleteTwoCommissions` |
| 探尋命運 3 次     | `PilgrimageDailyExploreFate3`           |

### 每週目標

| 遊戲條件              | Maa 任務                                   |
| --------------------- | ------------------------------------------ |
| 累計簽到 4 次         | 被動累積，不建立 Maa 任務                  |
| 參與公會討伐 7 次     | `PilgrimageWeeklyGuildRaid7`               |
| 探尋命運 30 次        | `PilgrimageWeeklyExploreFate30`            |
| 探索素材秘境 50 次    | `PilgrimageWeeklyMaterialRealm50`          |
| 領取 8 次日常副本獎勵 | `PilgrimageWeeklyClaimDailyDungeonReward8` |

### 本期目標

| 遊戲條件               | Maa 任務                                    |
| ---------------------- | ------------------------------------------- |
| 領取 30 次日常副本獎勵 | `PilgrimagePeriodClaimDailyDungeonReward30` |
| 累計簽到 10 次         | 被動累積，不建立 Maa 任務                   |
| 女神像祈願 40 次       | `PilgrimagePeriodGoddessPrayer40`           |
| 女神像祈願 240 次      | `PilgrimagePeriodGoddessPrayer240`          |
| 轉動扭蛋機 50 次       | `PilgrimagePeriodGacha50`                   |

巡禮中的登入、日常副本、祈願、探尋命運等重複條件，實作時會共用底層流程與進度判斷，不重複執行相同行為。

## 精彩活動：幸運刮刮樂

| 週期 | 遊戲條件            | Maa 任務                     |
| ---- | ------------------- | ---------------------------- |
| 每日 | 完成 3 次競技場挑戰 | `LuckyScratchArena3`         |
| 每日 | 轉動扭蛋機 2 次     | `LuckyScratchGacha2`         |
| 每日 | 消耗 60 點體力      | `LuckyScratchSpendStamina60` |
| 本期 | 完成日常副本 5 次   | `LuckyScratchDailyDungeon5`  |

## 精彩活動：秘寶大作戰

| 週期 | 遊戲條件                  | Maa 任務                              |
| ---- | ------------------------- | ------------------------------------- |
| 每日 | 今日上線登入              | 被動達成，不建立 Maa 任務             |
| 每日 | 完成日常副本 2 次         | `TreasureBattleDailyDungeon2`         |
| 每日 | 聖獸試煉領取第 3 階段獎勵 | `TreasureBattleClaimBeastTrialStage3` |
| 每日 | 家園商店購買 4 次         | `HomeShopPurchase`                    |

「購買禮包獲得」不是自動任務，不建立 Maa 任務。封印寶藏頁會消耗活動資源（實機畫面顯示每次 1200），也不納入自動領獎或自動兌換。

## 家園商店購物

`HomeShopPurchase` 是日常與秘寶大作戰共用的單一 Maa 任務，不為「購買 2 次」與「購買 4 次」重複建立任務。

- 每日雜貨刷新次數上限可設為 0～10 次，預設 3 次。
- 雜貨第一排可設定「全部購買」或「不購買」，預設全部購買。
- 粗煉石（黑色的鐵）預設 7 折以下購買。
- 稀有的時之沙（黃色的砂）預設 8 折以下購買。
- 歷戰精華（紫色）預設 9 折以下購買。
- 普通凍乾（咖啡色）預設 6 折以下購買。
- 寶庫前三排共九格，各自可設為不購買、1～9 折以下或不限折數；未指定前預設不購買。
- 特賣商品可設為不購買、1～9 折以下或不限折數；沒有特賣時直接略過，預設不購買。
- 雜貨判斷必須同時符合商品圖示與折數上限。購買動作只允許點擊該商品下方的黃色價格按鈕。
- 刷新後尚未購買的原始 `720×1280` 樣本保存在 `tests/screenshots/home-shop-grocery-top-refreshed.png`，四種雜貨圖示已拆到 `resource/base/image/home_shop/`。

2026-08-29 已在 BlueStacks 5（`emulator-5564`，720×1280）實機確認購買機制：點一下商品下方的黃色價格按鈕即成交，沒有二次確認彈窗；成交後該商品圖片區改顯示「售完」且整張卡片轉灰。刷新同樣是一點即刷，沒有確認彈窗。

### 版面

雜貨與寶庫共用同一組 3×3 格線（720×1280 直式座標）：

| 元素     | 中心座標                   |
| -------- | -------------------------- |
| 折數徽章 | `(38 + 229c, 200 + 283r)`  |
| 商品圖示 | `(132 + 228c, 293 + 282r)` |
| 售完標示 | `(132 + 228c, 297 + 282r)` |
| 價格按鈕 | `(132 + 228c, 410 + 282r)` |

### 雜貨

前三排 9 格是每日輪替的折扣商品，第四排以後是固定商品（無折數、無限購），流程不處理。

商品種類以圖示模板比對判定，共六種素材加上「其他」：木材、石塊、粗煉石、稀有時之沙、歷戰精華、普通凍乾。「其他」是第一排低機率出現的稀有商品（例如飾品），判定方式是「六種素材圖示都比不中」。

**圖示比對門檻必須 ≥ 0.85**：實測木材模板比對石塊會拿到 0.674，門檻設 0.6 會誤判成木材。

每一類都可獨立設定折數上限，共 12 種選擇：1～9 折以下、「不限折數（只要有折扣就買）」、「不限折數（含原價，一律購買）」、不購買。

前十種都靠折數徽章的 OCR 判斷，因此**沒有折數標籤的原價商品買不到**；要連原價一起買必須選「含原價，一律購買」，該模式改用 `DirectHit` 直接點價格按鈕，不看折數。

右上角可花金幣刷新，費用遞增（實測 4000 → 8000），刷新後會重新掃一輪。刷新次數上限由 `HomeShopRefreshLimit` 設定，預設不刷新。

### 寶庫

前九格商品固定、折數每日變動，使用晶石購買，每格可獨立設定折數上限，預設全部不購買。第 1 格通常是每日免費體力，由獨立的「免費」文字辨識自動領取。

**第 3 格每日限購 2 次**，其餘每格 1 次。購買後會回到該格重新檢查「售完」，因此限購多次的格子會連續買到上限或售完為止。

領完後按鈕文字仍是「免費」（只是轉灰），所以領取節點以 `max_hit: 3` 收斂，最多會多點兩次沒有作用的按鈕。

### 特賣

每日不定期出現，固定只有一個商品，使用晶石購買，可設定折數上限，預設不購買。

## 固定安全規則

- 家園商店的雜貨以金幣購買，預設依各素材的折數上限自動購買；寶庫與特賣使用晶石，預設全部不購買，需使用者自行開啟。刷新會花金幣且費用遞增，預設不刷新。
- 探尋命運等尚未確認免費條件與停止規則的任務，維持 `DoNothing` 骨架。
- 祈願、扭蛋、結緣已由使用者明確授權執行，並以畫面上的「今日已祈願／已開啟／已結緣」計數驗證實際執行結果。
- 任務獎勵只點擊「領取」或「全部領取」，不從領獎流程點擊「前往」。
- 月卡只允許點擊明確辨識到的「領取」；顯示「延長」、價格或購買文字時安全結束。
- 所有循環必須設定最大處理次數，找不到可靠狀態時安全結束。

## 日常任務導航確認

- 所有新增的遊戲功能統一放在 `DailyObjectives`（日常任務）群組。
- 「羈絆冒險」與「奇景委派」是同一個 Maa 任務；「委託任務」仍是另一個獨立任務。
- 「探尋命運」與「探索命運」是同一個 Maa 任務。
- 筆記主頁固定包含：日常副本、羈絆冒險、地圖探索、競技、每日活動、素材秘境、每週活動。
- 每日活動內已確認：聖獸試煉、雙影幻境、魔物戰爭。
- 競技場只處理競技入口的第一個模式；進入後在四個項目中固定選最右側，戰鬥 3 次後停止。
- 聖獸試煉與雙影幻境共用操作骨架：前往地圖、自動傳送、點人物上方入口、進入組隊、選擇夥伴、任選一人後進入副本。
- 選擇夥伴可能對其他玩家送出邀請，實機測試選人前必須再次確認。
- 頁面切換後必須先辨識新頁再進行下一次點擊，禁止用連續固定座標點擊。
- 公會放置獎勵爐位於公會主場景右下角；公會大廳內免費捐贈每日上限為 5 次。
- 公會捐贈可在「日常任務設定」或個別任務內設定 0～5 次，預設 0；第 1 次免費，後續允許消耗遊戲資源，不因按鈕顯示資源價格而停止。
- 素材秘境的鐵石礦洞、黃金礦洞、怪物洞窟與砂石礦洞，均可分別設定購買 0～20 次、快速開採開關及採集 0～999 次；購買預設 0。四礦洞安全設定已在 BS5 實機走完。
- 煉金爐分解可設定是否勾選「排除高戰力」，預設勾選；流程會先用模板比對辨識目前狀態，只有與設定不符時才點擊切換（兩個狀態模板的比對門檻必須用 0.95，0.75 會互相誤判）。
- 夜晚家園、筆記與公會定位素材已加入離線測試；早上、中午、黃昏素材後續加入同一組節點。

## 家園設施流程（2026-08-29 實機驗證）

Pipeline 位於 `resource/base/pipeline/home_facilities.json`。共同前置為：確認底部「家園」頁籤 → 向左滑 6 次把畫面推到最右側小屋（穩定錨點）→ 依需要向右滑回指定格。進入設施後一律以畫面左上角標題文字驗證，未進入則重試 3 次後安全停止。

| 設施         | 位置（由最右側算起） | 入口座標   | 標題驗證   | 執行內容                                                                                            |
| ------------ | -------------------- | ---------- | ---------- | --------------------------------------------------------------------------------------------------- |
| 煉金爐       | 最右側               | (565, 855) | 煉金爐     | 分解分頁：套用「排除高戰力」→ 勾選四個品級 → 分解 → 重複直到「已選物品」為 `0/`；寶石分頁：一鍵合成 |
| 道具（寶箱） | 最右側               | (95, 890)  | 道具       | 重複「一鍵使用」→「確認」，直到按鈕消失                                                             |
| 商店（推車） | 向右 1 格            | (245, 840) | 商店       | 雜貨第一排依折數購買、寶庫領免費                                                                    |
| 時光扭蛋機   | 向右 1 格            | (48, 835)  | 時光扭蛋機 | 切「國度」→ 扭蛋 1 次 × N                                                                           |
| 女神像       | 向右 2 格            | (645, 810) | 祈願       | 切「友情祈願」→ 祈願 1 次 × N                                                                       |
| 大櫻花樹     | 向右 2 格            | (380, 620) | 幻獸結緣   | 結緣 10 次 × N 或 結緣 1 次 × N                                                                     |

重要觀察：

- **這款遊戲不吃 Android 系統返回鍵（`ClickKey key: 4`）**。所有返回都必須點畫面左下角的返回按鈕（約 `(75, 1218)`）。在家園首頁時該座標是「角色」頁籤，因此收尾流程必須先判斷是否已在家園再決定要不要點。
- 祈願／扭蛋的按鈕數量會依貨幣餘額變動（例如代幣不足 1000 時「扭蛋 100 次」會消失），按鈕位置隨之改變，**必須用文字辨識而非固定座標**；`扭蛋1次` 不會誤配 `扭蛋10次`。
- 抽取結果畫面仍保留抽取按鈕，但 y 座標可能與主畫面不同，因此次數以 `max_hit` 自我迴圈（每輪重新辨識位置）實作，而非 `repeat`（`repeat` 不會重新辨識，會點到空白處）。
- 裝備分解有單次數量上限（實測 99 件），必須重複「勾選品級 → 分解」；`AlchemyDecomposeRounds` 為安全上限，預設 8 輪。
- 分解頁共 **6 個分類**，每列 5 個、第 6 個換到第二列：普通／稀有／史詩／傳說裝備、幻獸碎片、古遺物碎片。座標為 `y=961` 的 `x=114/246/377/509/641` 加上 `y=1011` 的 `x=98`。「古遺物碎片」只在部分帳號出現；沒有該分類時第二列是空白區，點下去沒有作用，所以固定點 6 個位置是安全的。
- 家園禮物泡泡**要點泡泡下方的 NPC 本體**（模板框往下偏移 55px），直接點泡泡本身不一定有反應。
- 分解頁的「排除高戰力」在每次重新進入時會回到勾選狀態。
- 「一鍵使用」單次上限 100 個；需要自行挑選內容的寶箱不會被一鍵使用。

## Custom Action Agent（次數計算）

MaaFramework 的 pipeline（JSON）沒有變數、算術與狀態，因此「輸入總次數自動拆成 100／10／1 抽」與「扣除今日已抽過的次數」無法用 pipeline 表達；`interface.json` 的 `input` 選項也只是純字串代換（`max_hit: "{Count}"`），沒有運算能力。

解法是 MaaFramework 的 Agent：MXU 依 `interface.json` 的 `agent` 設定啟動 `agent/main.py` 子行程，兩邊以本機 socket 溝通，pipeline 用 `"action": "Custom", "custom_action": "PullByCount"` 呼叫過去。

`PullByCount` 由幻獸結緣、女神像祈願、時光扭蛋機共用，參數以 `profile` 區分：

1. OCR 讀「今日已結緣／已祈願／已扭蛋（結果畫面寫「已開啟」）」計數。
2. 依 `deduct_done` 決定 total 是「今日累計目標」還是「本次要抽的次數」。
3. 貪心選用不會超抽的最大按鈕；按鈕會因貨幣不足而消失，找不到就退到較小的。
4. 每次點擊後重讀計數確認有進展，連續兩次沒動就判定資源不足並安全停止。

實作要點：

- Agent 子行程**不可**直接呼叫 `controller.post_click`（控制器在主程式端），必須用 `context.run_action_direct(JActionType.Click, JClick(...))` 代理。`post_screencap` 則可以直接用。
- 辨識結果的 `box` 在這個版本是 `[x, y, w, h]` 陣列而非 Rect 物件。
- Agent 要讓訊息出現在 MXU 日誌，可透過 `context.run_task("Agent.Log", {...focus...})` 推回通知管道；直接 print 只會留在子行程的 stdout。
- Python `maafw` 套件版本必須對上 MXU 使用的 MaaFramework 版本（目前 **v5.12.3**）。注意 `maa-tools` 下載的是另一個版本（v5.13.0-beta.5），拿它驗證不等於驗證了 MXU 的行為。

### 次數選項的覆寫寫法

「次數」與「是否扣除」拆成兩種機制，避免依賴 `pipeline_override` 的合併語意：

- 是否扣除 → 用 `next` 路由，`{P}.Pull.Route` 指向 `{P}.Pull.Deduct` 或 `{P}.Pull.Raw`。
- 次數 → 對 Deduct／Raw **兩個節點都寫入完整的** `custom_action_param`（含 `profile` 與 `deduct_done`），這樣不論覆寫是深層合併還是整包取代都正確。

## Pipeline 撰寫上的共同陷阱

`next` 只列一個候選時，該候選辨識失敗會讓**父節點連帶失敗**，整個任務中斷。已知踩過的例子：

- 設施進場後的 `Screen` 節點只指向一個後續動作 → 背包沒有可一鍵使用的道具時被判成任務失敗。
- 祈願池的 `Screen` 只指向 `Pool.Select` → 目標池被捲到畫面外就直接中斷。
- 刷新選項把 `RefreshRoute.next` 覆寫成只有 `Refresh` → 刷新次數用完後 `Refresh` 被 `max_hit` 跳過，`RefreshRoute` 失敗。

原則：**每個 `next` 都要有一條保證可走的收尾路徑**（DirectHit 的 Skip／Done／安全結束節點）。Custom Action 回傳失敗時另以 `on_error` 導回返回家園的節點。

## 公會五項流程（2026-08-30 實機驗證）

Pipeline 位於 `resource/base/pipeline/guild_facilities.json`。

**底部導航判斷**：只有「目前所在分頁」才會顯示文字，所以判斷所在頁一律用整條底列的 OCR
（`roi: [0, 1230, 720, 60]`，`expected: 公會`）。**不可用 `navigation/bottom-guild.png`**
——那是「未選中」狀態的圖示，在角色頁也會以 1.000 命中。`navigation/bottom-home.png` 則相反，
是選中狀態，但在其他分頁仍有 0.749，原本 0.75 的門檻只差 0.001，已一併調高到 0.90。

**場景捲動**：公會場景由左到右為 礦場／田園／羊圈 → … → 公會大廳／藏書庫／雞舍／遠征之門 →
能源台／冶煉工坊／倉庫／商店（右端）。以右端為錨點，往左移動 1 格到公會大廳與遠征之門。
捲動必須用一連串獨立的 Swipe 節點，**不能用單一節點加 `repeat`**：實測 repeat 連續滑動會
滑過頭，把畫面切換到別的主頁籤。

建築入口一律以建築名稱文字 OCR 定位，再往上偏移 40px 點到建築本體，比固定座標穩定。

| 項目         | 入口                                       | 關鍵行為                                                                                                                  |
| ------------ | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- |
| 公會放置獎勵 | 畫面右下角熔爐，固定位置不受捲動影響       | 一點即領；熔爐本體中心約 `(627, 970)`，左側 `521~580` 是角色頭像，點到會開啟「角色屬性」                                  |
| 公會捐贈     | 往左 1 格 → 公會大廳 → 公會捐贈            | 每日 5 次；第 1 次「免費捐贈」，第 2 次起消耗晨星（20 → 50 遞增）並跳出確認彈窗                                           |
| 公會討伐     | 往左 1 格 → 遠征之門 → 公會活動 → 公會討伐 | 每日 2 次；挑戰 → 開始戰鬥 → 等「戰鬥結束」（最多 50 回合，實測約 2～3 分鐘）→ 點空白處關閉                               |
| 公會商店     | 右端 → 商店                                | 前 11 項商品與折數固定，逐項設定不購買／買 1 次／買到限購上限；一點成交、無確認彈窗                                       |
| 冶煉工坊熔爐 | 右端 → 冶煉工坊 → 右下角「放置工具」       | 黑鐵（免費）／黃金（150 晨星）／結晶（450 晨星），每次 24 小時、上限 7 天；租借與續租都有確認彈窗，還要按「切換」才會生效 |

### 公會商店版面

捲到最上方時價格列 y = 405 / 700 / 996（第 1～9 項）；捲到最下方時第 10、11 項在 y = 786 的
第 1、2 欄。欄 x = 132 / 360 / 589，售完標示在價格上方 112px。第 12 項之後是無限購的固定商品，不處理。

### 熔爐按鈕

按鈕組合會隨狀態變動，必須用文字定位：未租借非使用中只有「租借」（置中）；已租借非使用中是
「續租」＋「切換」；已租借且使用中只有「續租」（置中）。所以「切換」的 ROI 要涵蓋整列按鈕。
剩餘時間顯示為「還可以使用N天HH小時MM分SS秒」，天數計算由 agent 的 `RenewFurnace` 負責。

## Pipeline 保底路徑與 `pnpm audit:pipeline`

MaaFramework 在節點執行完後會依序辨識 `next` 清單裡的候選；**全部失敗時這個節點就算失敗，並往上層傳播**，最後整個任務中止。因此「本來就沒東西可做」這種合理狀態，如果沒有保底候選，就會被回報成任務失敗。

實際踩過的例子：

- 背包沒有可一鍵使用的道具 → `Bag.Screen.next` 只有 `Loop.UseAll`，被判定成任務失敗。
- 目標祈願池被捲到畫面外 → `GoddessStatue.Screen.next` 只有 `Pool.Select`，直接中斷。
- 雜貨刷新次數用完 → `RefreshRoute.next` 被選項覆寫成只有 `Refresh`，`max_hit` 用盡後失敗。
- 特賣分頁當天沒出現 → 寶庫最後一格的 `next` 只有 `Sale.Tab`，整個商店任務失敗。

`tools/audit-pipeline.mjs`（`pnpm audit:pipeline`）會做兩件事：檢查 `next` / `on_error` 參照是否存在（缺少時以非零結束碼失敗），並列出「唯一候選且該候選不是 DirectHit」的高風險節點。後者是提示而非錯誤——導航與進場重試鏈本來就靠父節點的多個候選吸收失敗，已排除在外；其餘要逐一判斷「這一步的辨識有沒有可能合理地失敗」。

## 下一輪待確認

1. 委託列表的實際項目與完成條件。
2. 日常副本的入口、免費次數、體力消耗、掃蕩與戰鬥結束狀態。
3. 家園商店：刷新次數用完後遊戲端的實際反應（目前僅以 `max_hit` 收斂，未辨識剩餘次數）。
4. 競技場、聖獸試煉、雙影幻境、公會討伐、素材秘境的進場與停止規則。
5. 探尋命運的免費次數辨識。
6. 煉金爐分解迴圈的多輪回歸（驗證當下帳號已無可分解裝備，僅逐一驗證各節點辨識，未跑滿多輪）。

原始採集圖可能包含玩家名稱與聊天，不可直接加入回歸測試；正式測試素材必須先遮蔽個人資訊。
